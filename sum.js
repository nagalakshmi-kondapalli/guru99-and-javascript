/* Write a Program to take a 2 digit number and calculate sum of digits. */
no = parseInt(prompt("Enter two digit number:"));
        q = no/10;
        r = no%10;
        sum = q + r;
        alert("double digit sum"+sum);
